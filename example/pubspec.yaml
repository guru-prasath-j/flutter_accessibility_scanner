name: flutter_accessibility_scanner_example
description: Example app demonstrating flutter_accessibility_scanner usage.
version: 1.1.0+1
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_accessibility_scanner:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
